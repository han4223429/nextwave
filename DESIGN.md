# NextWave — 함께 만드는 다음 물결

‘작은 시작이, 다음 물결로.’ 서로 다른 사람의 아이디어가 함께 성장하는 창업동아리를 밝은 수면과 직접 만지는 물결로 표현합니다. 첫 화면은 한글 메시지와 여백을 중심으로 구성하고, 실제 프로젝트 체험·활동·사람·합류 안내로 이어집니다. 회원 업무는 별도 LIVE DESK에 모읍니다.

## 색과 구성

- 미스트 시안 `#edfaff`, 밝은 지면 `#f7fcff`, 딥틸 글자 `#073a50`, 블루 `#087fc4`, 아쿠아 `#49d9ce`. 흰빛과 물의 투명도를 기본으로 삼고 소개·모집 구간에서 짙은 바다색을 사용합니다.
- 큰 한글 제목, 짧은 영문 보조 문구, 넓은 여백, 부드러운 곡면과 수면빛. 물결은 오른쪽과 아래에 집중하고 왼쪽 메시지를 읽을 공간을 확보합니다. 모바일에서는 프로젝트와 활동 내용을 한 열로 전환합니다.
- 홈은 Space Grotesk·Pretendard와 시스템 고정폭 글꼴, 포털은 Pretendard·JetBrains Mono를 사용합니다. 외부 폰트가 없어도 시스템 폰트로 표시합니다.
- 홈 토큰은 `styles.css`, 포털 토큰은 `portal.css`에서 관리합니다. 별도 프런트엔드 빌드 없이 정적 HTML·CSS·JavaScript로 동작합니다. 메인 수면은 WebGL, RESCUE JET은 김한용의 공개 포트폴리오와 동일한 3D 모델·뷰어를 옮겼습니다. 원본 설계 이미지에서 ‘3D로 살펴보기’를 누르면 회전·확대·추진 데모·분해 보기·초기화를 사용할 수 있습니다. 포트폴리오의 모델·재질·카메라·기본 동작을 유지하고 NextWave의 탭·언어·모션 설정에 연결합니다. 실제 장치 제어나 정량 성능 시뮬레이션으로 표현하지 않습니다. 제작자 귀속과 근거는 [프로젝트 출처](docs/project-sources.md)를 따릅니다.

## 활동과 부원 공간

관심 트랙 BUILD / PLAY / SOLVE / CONNECT는 실험 제안을 바꾸고 해당 분야의 포털 공고로 연결합니다. 제안 문구를 기존 수상 프로젝트로 표시하지 않습니다. 운영진은 네이티브 dialog로, FAQ는 details/summary로 엽니다. 홈의 한/영 문구는 `i18n.js`의 `water.*` 키와 기존 키를 사용하고 RESCUE 체험 문구는 해당 모듈에서 관리합니다.

LIVE DESK는 로그인 전에도 출처가 있는 기회를 검색·필터링하는 공간입니다. 부원 승인 후 채팅·공지·출석·회원 명단이 열립니다. 홈과 같은 시안·딥틸·블루·아쿠아를 사용하면서 공고를 빠르게 읽을 수 있는 정보 밀도를 유지합니다.

## 물의 소리

첫 조작부터 짧은 물방울·잔물결·물소리와 맑은 화음이 반응합니다. 배경 반복음 없이 클릭, 수면 터치·드래그, 프로젝트 선택, 의도적인 섹션 스크롤에 연결합니다. `water-audio.js`가 Web Audio로 소리를 만들고 `sound-ui.js`가 홈·포털의 같은 SOUND 설정을 관리합니다. 음소거는 저장되며 탭이 숨겨지면 재생과 예약 음을 멈춥니다. 낮은 기본 음량, 반복 간격, 최대 네 효과 제한으로 여러 조작이 겹쳐도 소리가 쌓이지 않게 합니다.

## 신뢰와 상태

기존 수상 기록, 실제 운영진, 프로젝트 귀속을 보존하고 현재 인원·모집 일정·성과 수치를 추정하지 않습니다. 모집 폼이 없으면 준비 중으로 표시합니다. 포털은 실제 마지막 성공 시각과 출처별 오류를 보여주며, 마감일 미확인을 상시 모집으로 바꾸지 않습니다. 새로고침은 마지막 수집본을 읽는 동작입니다. 이번 자연 수면의 데스크톱·모바일 Chrome 검증은 [수면 QA](docs/QA-WATER-2026-09-06.md)와 [효과음·최종 점검](docs/QA-SOUND-2026-09-07.md)에 기록했습니다. 실제 Google 인증의 미완료 상태는 화면·모션 검증과 구분합니다.
